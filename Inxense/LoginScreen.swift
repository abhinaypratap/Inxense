import SwiftUI

struct LoginScreen: View {
    var body: some View {
        VStack {
            Text("Hello, world!")
        }
    }
}

struct LoginScreen_Previews: PreviewProvider {
    static var previews: some View {
        LoginScreen()
    }
}
